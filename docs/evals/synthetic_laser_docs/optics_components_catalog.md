# Synthetic Optics Components Catalog

This synthetic document is for LaserClaw benchmark and resume metrics only. It is not a real vendor catalog or purchasing recommendation.

## OPT-MIR-OC1064

For a 1064 nm output coupler with T=5 and ROC=100, use the synthetic part number MIR-OC1064-T05. The part is intended for a linear or ring resonator output position.

## OPT-LBO-532

When LBO frequency doubling produces no 532 nm output, first check fundamental 1064 nm power, polarization, LBO angle, crystal temperature, and cavity alignment. Do not increase power until the root cause is isolated.

## OPT-SPECTRUM-532

For 532 nm spectrum measurement, use filter FLT-BP-532 before the spectrometer. The synthetic spectrometer identifier is SPEC-USB. Check integration time to avoid detector saturation.

## OPT-BLOCK-IR

Temporary containment of near-infrared beams should use beam block BB-BLOCK-IR. The block must be placed downstream of the diagnostic pickoff before opening shutters.

## OPT-CAM-BEAM

Before using beam camera CAM-BEAM, add ND attenuation at the front end. ND attenuation prevents sensor saturation and protects the camera during high-power alignment.

## Chinese Component Summary

1064 nm 输出耦合镜 T=5 ROC=100 的合成型号是 MIR-OC1064-T05。LBO 无 532 nm 输出时，优先检查基频功率、偏振、LBO 角度、温度和腔体准直。
